export enum PolygonEvent {
  Reduction = "reduction",
  Expansion = "expansion",
  NoChange = "noChange",
}

export enum PositionType {
  Vertex = "vertex",
  Internal = "internal",
  OnEdge = "onEdge",
  Other = "other",
}